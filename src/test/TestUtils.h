#pragma once

#include <vector>
#include <string>

extern const char* OK_FLAG;
extern const char* CHK_FLAG;
extern const char* INFO_FLAG;
extern const std::vector<std::string> TEST_FILE_LIST; // 记录所有测试用的输入文件的文件路径